# Matches Feature

Owns mutual match list, match profile summary, and chat entry points.

First screens:

- `MatchListScreen`
- `MatchDetailScreen`

State:

- `MatchesController`
- `MatchRepository`

API:

- `GET /matches`
- `GET /matches/:matchId`
