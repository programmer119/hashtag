# Mobile Tests

Add tests after the generated Flutter project lands.

First test targets:

- Splash redirects by session and approval state.
- Auth verification success/failure states.
- Profile draft validation.
- Swipe like/pass optimistic updates.
- Wallet unlock prevents duplicate submissions.
